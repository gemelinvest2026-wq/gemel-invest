{
  "name": "gemel-invest-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "GEMEL INVEST mobile shell powered by Capacitor",
  "scripts": {
    "sync": "npx cap sync",
    "copy": "npx cap copy",
    "android:add": "npx cap add android",
    "android:open": "npx cap open android",
    "ios:add": "npx cap add ios",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0"
  }
}